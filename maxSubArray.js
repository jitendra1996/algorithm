const maxSubArray = (arr) => {
    let sum = Number.NEGATIVE_INFINITY;
    const resultArray = [];

    for(let i = 0 ; i < arr.length ; i++){
        let j = i + 1 ;
        let loop = i ;
        let newSum = 0;
        while(j < arr.length){
            
        }
    }
}